{
    "name": "smart-traffic-frontend",
    "version": "2.0.0",
    "description": "Smart Traffic Monitoring System — Frontend (Plain HTML/CSS/JS)",
    "private": true,
    "scripts": {
        "start": "npx serve . -p 5500",
        "dev": "npx live-server --port=5500 --host=127.0.0.1 --open=/index.html"
    },
    "devDependencies": {
        "live-server": "^1.2.2",
        "serve": "^14.2.0"
    },
    "cdnDependencies": {
        "_note": "Các thư viện load qua CDN, không cần cài npm",
        "chart.js": "https://cdn.jsdelivr.net/npm/chart.js",
        "leaflet": "https://unpkg.com/leaflet/dist/leaflet.js",
        "Google Fonts (Inter)": "https://fonts.googleapis.com/css2?family=Inter"
    }
}