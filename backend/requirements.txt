# ─────────────────────────────────────────────
# Web Framework
# ─────────────────────────────────────────────
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
starlette>=0.41.0             # SessionMiddleware (dùng bởi authlib OAuth)

# ─────────────────────────────────────────────
# Database
# ─────────────────────────────────────────────
sqlalchemy[asyncio]>=2.0.0
asyncpg>=0.29.0               # async PostgreSQL driver cho FastAPI
psycopg2-binary>=2.9.0        # sync driver cho Alembic migration
alembic>=1.13.0

# ─────────────────────────────────────────────
# Authentication
# ─────────────────────────────────────────────
authlib>=1.3.0                # Google OAuth2 (authorize_redirect, authorize_access_token)
httpx>=0.27.0                 # HTTP client dùng bởi authlib
python-jose[cryptography]>=3.3.0  # JWT encode/decode
itsdangerous>=2.2.0           # SessionMiddleware secret

# ─────────────────────────────────────────────
# Config & Utilities
# ─────────────────────────────────────────────
pydantic-settings>=2.0.0      # BaseSettings đọc .env
python-dotenv>=1.0.0
overrides>=7.7.0              # @override decorator (dùng trong detector.py)

# ─────────────────────────────────────────────
# AI / ML — Video Processing
# ─────────────────────────────────────────────
ultralytics>=8.3.0            # YOLO v8 + ByteTrack + SpeedEstimator
opencv-python>=4.9.0          # xử lý ảnh / video
numpy>=1.26.0
cvzone>=1.6.1                 # vẽ text/box lên frame

# ─────────────────────────────────────────────
# AI Chatbot — LangGraph ReAct Agent
# ─────────────────────────────────────────────
langchain-core>=0.3.0         # base types: messages, tools, runnables
langchain-google-genai>=2.0.0 # ChatGoogleGenerativeAI (Gemini)
langgraph>=0.3.0              # create_react_agent, InMemorySaver
langchain>=0.3.0              # @tool decorator, PromptTemplate

# ─────────────────────────────────────────────
# System Monitoring (Admin API)
# ─────────────────────────────────────────────
psutil>=6.0.0

# ─────────────────────────────────────────────
# Testing
# ─────────────────────────────────────────────
pytest>=8.0.0
pytest-asyncio>=0.23.0
